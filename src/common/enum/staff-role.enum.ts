/** Staff roles for the admin dashboard. Students have no role. */
export enum StaffRole {
  ADMIN = 'admin',
  CREATOR = 'creator',
  REVIEWER = 'reviewer',
}

export enum AccountStatus {
  ACTIVE = 'active',
  SUSPENDED = 'suspended',
}
